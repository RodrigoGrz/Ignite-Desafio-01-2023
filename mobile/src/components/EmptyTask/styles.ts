import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
    },
    image: {
        marginBottom: 16,
    },
    strongText: {
        color: '#808080',
        fontSize: 14,
        fontWeight: 'bold',
    },
    normalText: {
        color: '#808080',
        fontSize: 14,
    },
}); 